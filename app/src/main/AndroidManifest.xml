<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ilgwon.alarmbomb">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SET_TIME"/>
    <uses-permission android:name="android.permission.SET_TIME_ZONE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".user_interface.AlarmSettingActivity"
            android:label="ALARM BOMB LOGIN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".Login_activity"
            android:label="ALARM BOMB"></activity>-->
        <!--<activity android:name=".Register_act"></activity>-->
        <!--<activity-->
            <!--android:name=".AlarmSettingActivity"-->
            <!--android:label="ALARM BOMB LOGIN">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        <!--</activity>-->
        <!--<activity android:name=".MainActivity"></activity>-->

        <activity android:name=".user_interface.Login_activity"></activity>
        <activity android:name=".user_interface.AlarmShowActivity" android:noHistory="true"></activity>
        <activity android:name=".user_interface.AlarmAddActivity" android:noHistory="true"></activity>
    </application>

<!--
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
-->

</manifest>